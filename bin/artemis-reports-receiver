#! /usr/bin/env perl

use strict;
use warnings;

use Artemis::Reports::Receiver;

Artemis::Reports::Receiver->run(
                                port         => 7357,
                                log_level    => 2,
                                max_servers  => 2,
                                max_requests => 2,
                               );

